import java.io.*;
import java.util.*;
import java.text.*;
import java.math.*;
import java.util.regex.*;
class Node
{
    int data;
    Node left;
    Node right;
    Node(int data)
    {
        this.data=data;
        this.left=null;
        this.right=null;
    }
}
class B
{
   public static Node build(Node root,int b)
   {
       if(root == null)
       {
           root = new Node(b);
           return root;
       }
       if(b>root.data)
       {
           root.right=build(root.right,b);
       }
       else if(b<root.data)
       {
           root.left=build(root.left,b);
       }
       return root;
   }
   public static Node check(Node root,int c,int d)
   {
       if(root == null)
       {
           return null;
       }
       if(root.data> c && root.data> d)
       {
          return check(root.left,c,d);
       }
       else if(root.data< c && root.data<d)
       {
          return check(root.right,c,d);
       }
       else
       {
           return root;
       }
   }
}
public class Solution {

    public static void main(String[] args) {
        /* Enter your code here. Read input from STDIN. Print output to STDOUT. Your class should be named Solution. */
        Scanner sc = new Scanner(System.in);
        int a = sc.nextInt();
        Node root = null;
        for(int i=0;i<a;i++)
        {
            int b=sc.nextInt();
            root = B.build(root,b);
        }
        int c =sc.nextInt();
        int d =sc.nextInt();
        Node an = B.check(root,c,d);
        System.out.print(an.data);
    }
}
