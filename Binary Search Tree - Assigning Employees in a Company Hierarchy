import java.io.*;
import java.util.*;
import java.text.*;
import java.math.*;
import java.util.regex.*;
class Node
{
    int data;
    Node left;
    Node right;
    Node(int data)
    {
        this.data=data;
        this.left=null;
        this.right=null;
    }
}
class B
{
    public static Node build(Node root, int b)
    {
        if(root == null)
        {
            root = new Node(b);
            return root;
        }
        if(b>root.data)
        {
            root.right = build(root.right,b);
        }
        if(b<root.data)
        {
            root.left = build(root.left,b);
        }
        return root;
    }
     public static void check(List<Integer> l, Node root)
     {
         if(root == null)
         {
             return;
         }
            check(l,root.left);
         l.add(root.data);
         check(l,root.right);
     }
}
public class Solution {

    public static void main(String[] args) {
        /* Enter your code here. Read input from STDIN. Print output to STDOUT. Your class should be named Solution. */
        Scanner sc =new Scanner(System.in);
        int n = sc.nextInt();
        Node root = null;
        for(int i=0;i<n;i++)
        {
            int b = sc.nextInt();
            root = B.build(root,b);
        }
        int c = sc.nextInt();
        List<Integer> l = new ArrayList<>();
        B.check(l,root);
       int i = l.indexOf(c);
        if(i == -1 || i == l.size()-1)
        {
            System.out.print("No Inorder Successor");
        }
        else
        {
        System.out.print(l.get(i+1));
        }
    }
}
